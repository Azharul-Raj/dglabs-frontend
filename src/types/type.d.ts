export type PropsType={
    darkMode:boolean;
}
export type userProps={
    name:string;
    email:string;
}
export type userDataType={
    status:string;
    data:{
        name:string;
        email:string
    }
}